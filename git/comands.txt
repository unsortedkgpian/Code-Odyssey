git log --oneline --> git history

git config --list ---> full settings

git config --global


git --version

git status --> Run everytime before gitstatus
:

git init

git show HEAD 

git show user

git commit open the vim -> nvim


Amending commit 
git commit --amend -> just one commit ago
git commit -a 

git branch -- list all branch * -> active/current

switching branches
git switch <branchname>

HEAD -> points to latest commit 

git branch -d <branchname>-> delete

git branch -M <branchname> -> rename

Merging 
first switch to branch i want to merge in
git merge <brancchname i want to merge>
git 

// Merging will done edit the file and fix it and save commit 

"<<<<< HEAD"
"========"
">>>>>> bugfix"

git checkout -b ksjdkfjs
tell the to create new branch


git diff -> unsaved and last commit 
git diff a_commit b_commit 
git diff HEAD 


stashing 
save the uncommit changes and allow to change the branch it will work 

git stash 
git stash pop

git stash apply-> multiple branches

git stash multiple 14:28
git stash list 

git stash apply stash@{2}




// time traveling with git checkout

git log 
git log --oneline 
git status 

// first seven digit requires only 

git checkout <7digit>

HEAD points out to the current branch in which are and the master points to current commit 


when we checkout to some commit it points directlry to the commit 

// different syntex 
git chekout HEAD~1 
// here 1 mean before one commit before HEAD 

git chekout branch directlry tranfer u branch to head to lestest thing

git switch - 
// to the latest branch


git checkout HEAD <filename> -> discarding changes 

git checkout -- <filename> -> discarding changes

git restore <filename> -> undoing changes 
git restore --source HEAD~1 <filename>


git restore --staged <filename> 
// unstaged the file  opposite of git add .


git restore <commit> -> remove all the commit and store the file 
git restore --hard <commit> -> remove evelryting till the commit 
git restore --hard HEAD~1 


// REVERT make a new commit -> and restore all the changes it mean we havn't lost anything
git commit <commit-hash>


git tags 

v1.1.23 is pointer to git commit  

lightweight tags and annotated tags

git hash-object <file> 
gitt hash-object --stdin 
-w -> store

git cat-file -p <object_hash> 


// Reflog ----> live saver 

git reflog show HEAD 
