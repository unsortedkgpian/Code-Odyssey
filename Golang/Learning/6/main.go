package  main
import "fmt"
func main(){
	age := 18

	if age >= 18{
		fmt.Println("person is an adult\n\n")
	}


	var role = "admin"
	var hasPermission = false

	if role == "admin" && hasPermission{
		fmt.Println("yes")
	}else {
		fmt.Println("Now conditions are wrong")
	}

	if pick:=54;pick>=50{
		fmt.Println("OK it workinng")
	}
}
